apiVersion: v1
kind: Service
metadata:
  name: {{ include "weather.fullname" . }}
spec:
  selector:
    app: {{ include "weather.fullname" . }}
  ports:
    - name: http
      port: 80
      targetPort: 8000
